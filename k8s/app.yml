apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: app-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: app
    spec:
      containers:
      - name: app
        image: eu.gcr.io/clustereurope2018/symfony2_demo:app
        ports:
        - containerPort: 80
